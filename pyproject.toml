[tool.ruff]
target-version = "py310"
line-length = 100
extend-exclude = [
  "state",
  "debug",
  "output",
  "node_modules",
  "dist",
]

[tool.ruff.lint]
select = ["E", "F", "I", "UP"]
ignore = ["E501"]

[tool.ruff.lint.isort]
known-first-party = ["helpers", "scripts"]
